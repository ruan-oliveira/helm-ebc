apiVersion: v1
kind: Service
metadata:
  name: {{ include "aula.fullname" . }}
  labels:
    {{- include "aula.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aula.selectorLabels" . | nindent 4 }}
